\documentclass[12pt, a4paper]{article}

%% ─── Paquetes ──────────────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[spanish]{babel}
\usepackage{lmodern}
\usepackage{geometry}
\usepackage{xcolor}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{microtype}
\usepackage{parskip}
% Pandoc 3.x usa \LTcaptype{none} en tablas; definir el contador evita el error
\newcounter{none}

%% ─── Geometría de página ───────────────────────────────────────────────────
\geometry{
  top=2.8cm,
  bottom=2.5cm,
  left=2.8cm,
  right=2.8cm,
  headheight=1.5cm
}

%% ─── Colores UCuenca ───────────────────────────────────────────────────────
\definecolor{ucazul}{HTML}{0A2464}   %% Azul marino institucional
\definecolor{ucrojo}{HTML}{C41230}   %% Rojo de acento
\definecolor{ucgris}{HTML}{6B6B6B}   %% Gris para texto secundario
\definecolor{uclightgray}{HTML}{F4F4F4}

%% ─── Hiperlinks ────────────────────────────────────────────────────────────
\hypersetup{
  colorlinks=true,
  linkcolor=ucazul,
  urlcolor=ucrojo,
  citecolor=ucazul
}

%% ─── Encabezado y pie de página ────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyhead[R]{\small\textls[200]{\textcolor{ucgris}{\textsf{UNIVERSIDAD DE CUENCA}}}}
\fancyfoot[R]{\small\textcolor{ucgris}{\thepage}}
\fancypagestyle{plain}{
  \fancyhf{}
  \fancyhead[R]{\small\textls[200]{\textcolor{ucgris}{\textsf{UNIVERSIDAD DE CUENCA}}}}
  \fancyfoot[R]{\small\textcolor{ucgris}{\thepage}}
  \renewcommand{\headrulewidth}{0pt}
}

%% ─── Tracking (letra espaciada) ────────────────────────────────────────────
\usepackage{letterspace}

%% ─── Títulos ───────────────────────────────────────────────────────────────
\titleformat{\section}
  {\Large\bfseries\sffamily\color{ucazul}}
  {}
  {0em}
  {}
  [\vspace{-0.3em}\textcolor{ucrojo}{\rule{\linewidth}{1.5pt}}]

\titleformat{\subsection}
  {\large\bfseries\sffamily\color{ucazul}}
  {}
  {0em}
  {}

\titleformat{\subsubsection}
  {\normalsize\bfseries\sffamily\color{ucgris}}
  {}
  {0em}
  {}

\titlespacing*{\section}{0pt}{1.5em}{0.8em}
\titlespacing*{\subsection}{0pt}{1.2em}{0.5em}

%% ─── Listas ────────────────────────────────────────────────────────────────
\setlist[itemize]{
  leftmargin=1.5em,
  itemsep=0.3em,
  label=\textcolor{ucrojo}{\textbullet}
}

%% ─── Tabla ─────────────────────────────────────────────────────────────────
\renewcommand{\arraystretch}{1.3}

%% ─── Línea decorativa bajo el título del documento ────────────────────────
\newcommand{\ucreglafull}{%
  \noindent\textcolor{ucazul}{\rule{\linewidth}{3pt}}%
  \vspace{0.5em}
}

%% ─── Metadatos desde Pandoc ────────────────────────────────────────────────
$if(title)$
\title{%
  \vspace{-1cm}
  {\sffamily\bfseries\Huge\color{ucazul} $title$}
}
$endif$
$if(author)$
\author{\sffamily\color{ucgris} $for(author)$$author$$sep$ \and $endfor$}
$endif$
$if(date)$
\date{\sffamily\color{ucgris} $date$}
$endif$

%% ─── Comandos Pandoc ───────────────────────────────────────────────────────
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
%% Escalado automático de imágenes (Pandoc default template approach)
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>0.80\textheight0.80\textheight\else\Gin@nat@height\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
%% pandocbounded: Pandoc 3.x envuelve includegraphics; con setkeys ya está limitado
\providecommand{\pandocbounded}[1]{#1}
\makeatletter
\def\fps@figure{htbp}
\makeatother

%% ═══════════════════════════════════════════════════════════════════════════
\begin{document}

%% ─── Portada ───────────────────────────────────────────────────────────────
$if(title)$
\begin{titlepage}
  \pagecolor{ucazul}
  \color{white}
  \vspace*{4cm}
  \begin{center}
    {\sffamily\fontsize{11}{13}\selectfont\textls[300]{UNIVERSIDAD DE CUENCA}}\\[1.5em]
    \textcolor{white}{\rule{8cm}{0.5pt}}\\[2em]
    {\sffamily\bfseries\fontsize{32}{38}\selectfont $title$}\\[1.5em]
    $if(author)$
      {\sffamily\fontsize{13}{16}\selectfont $for(author)$$author$$sep$ \\ $endfor$}\\[0.8em]
    $endif$
    $if(date)$
      {\sffamily\fontsize{11}{13}\selectfont\textcolor{white!70!ucazul} $date$}
    $endif$
  \end{center}
  \vfill
  \begin{center}
    \textcolor{white!40!ucazul}{\rule{12cm}{0.3pt}}
  \end{center}
\end{titlepage}
\nopagecolor
$endif$

%% ─── Tabla de contenidos (si se solicita) ──────────────────────────────────
$if(toc)$
\newpage
{
  \hypersetup{linkcolor=ucazul}
  \tableofcontents
  \newpage
}
$endif$

%% ─── Cuerpo del documento ──────────────────────────────────────────────────
$body$

\end{document}
